# C-Language-Projects
🚀 Learning and experimenting with C programming entirely in the cloud using GitHub Code spaces. No local setup required!
# ⚡ Cloud C Programming Workspace

Welcome! This repository tracks my journey learning and building applications in **C**, developed entirely within the cloud using **GitHub Codespaces**. 

No local compilers, external IDE downloads, or environment configurations required—just pure C in the browser.

---

## 📂 Project Directory

- `hello.c` – Standard starter script to verify environment configuration.
- `calculator.c` – Basic command-line arithmetic utility.
- *(Add your new `.c` files here as you build them!)*

---

## 🚀 How to Run Code in This Workspace

If you are running this in GitHub Codespaces, open the terminal (`Ctrl + ~`) and use `gcc`:

### 1. Compile the Program
```bash
gcc filename.c -o program
